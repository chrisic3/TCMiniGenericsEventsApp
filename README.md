# TCMiniGenericsEventsApp

Demo of Events and Generics. 

Takes two lists and writes them to csv files. Checks for bad words and throws an event if it finds one. The event handler writes the error to the console.

Tim Corey - C# Mastercourse